diff --git a/node_modules/next/dist/client/app-index.js b/node_modules/next/dist/client/app-index.js
index e454a54..33de31b 100644
--- a/node_modules/next/dist/client/app-index.js
+++ b/node_modules/next/dist/client/app-index.js
@@ -133,10 +133,10 @@
     setTimeout(DOMContentLoaded);
 }
 const nextServerDataLoadingGlobal = self.__next_f = self.__next_f || [];
-// Consume all buffered chunks and clear the global data array right after to release memory.
-// Otherwise it will be retained indefinitely.
+// Consume all buffered chunks. Do NOT clear the array: Turbopack HMR
+// re-executes this module during initial compilation; keeping entries allows
+// re-executions to rebuild the RSC stream rather than hanging on an empty one.
 nextServerDataLoadingGlobal.forEach(nextServerDataCallback);
-nextServerDataLoadingGlobal.length = 0;
 // Patch its push method so subsequent chunks are handled (but not actually pushed to the array).
 nextServerDataLoadingGlobal.push = nextServerDataCallback;
 let readable = new ReadableStream({
@@ -193,7 +193,6 @@
     initialServerResponse = createFromReadableStream(readable, {
         callServer: _appcallserver.callServer,
         findSourceMapURL: _appfindsourcemapurl.findSourceMapURL,
-        debugChannel,
         startTime: 0
     });
 }
@@ -237,6 +236,8 @@
     onUncaughtError: _errorboundarycallbacks.onUncaughtError
 };
 async function hydrate(instrumentationHooks, assetPrefix) {
+    if (self.__next_hydrated) return;
+    self.__next_hydrated = true;
     let staticIndicatorState;
     let webSocket;
     if (process.env.__NEXT_DEV_SERVER) {
diff --git a/node_modules/next/dist/client/components/router-reducer/fetch-server-response.js b/node_modules/next/dist/client/components/router-reducer/fetch-server-response.js
index abc1234..def5678 100644
--- a/node_modules/next/dist/client/components/router-reducer/fetch-server-response.js
+++ b/node_modules/next/dist/client/components/router-reducer/fetch-server-response.js
@@ -413,9 +413,11 @@
 function createFromNextFetch(promiseForResponse, requestHeaders) {
     return createFromFetch(promiseForResponse, {
         callServer: _appcallserver.callServer,
         findSourceMapURL: _appfindsourcemapurl.findSourceMapURL,
-        debugChannel: createDebugChannel && createDebugChannel(requestHeaders)
+        // Omit debugChannel: the dual-stream close() gate (RSC body +
+        // debug channel both must finish) can prevent close() from firing
+        // during navigation, leaving module-chunk deps pending indefinitely.
     });
 }
diff --git a/node_modules/next/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js b/node_modules/next/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js
index c3188d9..a7f3c12 100644
--- a/node_modules/next/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js
+++ b/node_modules/next/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js
@@ -4733,8 +4733,8 @@
               (response.status = "halted"),
               (response.value = null),
               (response.reason = null))
-            : (chunk = new ReactPromise(
-                "rejected",
-                null,
-                response._closedReason
-              ))
+            : (chunk = new ReactPromise(
+                "rejected",
+                null,
+                response._closedReason
+              ))
           : (chunk = createPendingChunk(response)),
         chunks.set(id, chunk));
       return chunk;
@@ -4737,6 +4737,8 @@
               (response._debugChannel = void 0),
               null !== debugChannelRegistry &&
                 debugChannelRegistry.unregister(response)))
-          : reportGlobalError(weakResponse, Error("Connection closed."));
+          : (response._closed = !0);
+        // Turbopack compiles client-component modules after stream EOF;
+        // _pendingChunks reaches 0 when they finish, resolving chunks naturally.
       }
     }
diff --git a/node_modules/next/dist/server/app-render/parse-and-validate-flight-router-state.js b/node_modules/next/dist/server/app-render/parse-and-validate-flight-router-state.js
index 2f0b64c..39f060d 100644
--- a/node_modules/next/dist/server/app-render/parse-and-validate-flight-router-state.js
+++ b/node_modules/next/dist/server/app-render/parse-and-validate-flight-router-state.js
@@ -38,11 +38,7 @@ function parseAndValidateFlightRouterState(stateHeader) {
         (0, _superstruct.assert)(state, _types.flightRouterStateSchema);
         return state;
     } catch  {
-        throw Object.defineProperty(new Error('The router state header was sent but could not be parsed.'), "__NEXT_ERROR_CODE", {
-            value: "E10",
-            enumerable: false,
-            configurable: true
-        });
+        return undefined;
     }
 }

